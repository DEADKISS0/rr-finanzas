# Capa DB — DashWeb

Cliente y tipos para la base unificada (Supabase).

- `supabase.ts` — cliente server
- `types.ts` — entidades, proyectos, snapshots
- `fetch.ts` — helpers del dashboard

APIs: `src/app/api/db/`
UI CRM: `src/app/crm/`

Setup: `RR/ChatBot/bot_telegram/db/README.md`
